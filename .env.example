# ThousandEyes API connection.
#
# The YAML config file (thousand-eyes-mcp.yaml) is OPTIONAL: setting the
# variables below — exported in your shell or placed in this .env — is enough
# to run the server, which is what makes it work when installed via
# `uv tool install` and launched by an MCP client. Config precedence, highest
# first: CLI flags > environment variables > YAML file > built-in defaults.
#
# Bearer token: create one at
#   https://app.thousandeyes.com → Account Settings → Users and Roles
#   → Profile → User API Tokens
THOUSANDEYES_BEARER_TOKEN=

# Optional — default account group ID injected as the `aid` query param on
# every request that accepts it. Leave blank to use the token's home account.
THOUSANDEYES_ACCOUNT_GROUP_ID=

# Optional — set to false to disable upstream TLS verification (not recommended).
# THOUSANDEYES_VERIFY_SSL=true

# MCP server transport bearer token (only used when transport.auth.type=bearer in thousand-eyes-mcp.yaml)
# THOUSANDEYES_MCP_TOKEN=

# Debug capture (off by default) — when on, a failed tool call attaches the
# exact upstream request/response to the error result and logs one JSON line to
# stderr, so opaque errors can be diagnosed from the facts of the exchange.
#   THOUSANDEYES_MCP_DEBUG=true            # enable (0/false disables)
#   THOUSANDEYES_MCP_DEBUG_CAPTURE=errors  # "errors" (default) or "all"
#   THOUSANDEYES_MCP_DEBUG_REDACT=true     # mask auth headers + credential-shaped values (default on)
# Equivalent CLI flags: --debug / --debug-all-calls / --debug-no-redact.
